#include <stdio.h>

int main(){
    int n, n2, confere;
    
    confere = 1;
    
    scanf("%d", &n);
    
    while (n != 0){
        scanf("%d", &n2);
        if (n*n!=n2){
            confere = 0;
        }
        scanf("%d", &n);
    }
    
    printf("%d\n", confere);
}
